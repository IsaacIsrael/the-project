name: 🛠️ Environment setup
description: Setup Node, Ruby + Bundler (CocoaPods via Gemfile), npm cache

inputs:
  setup_node:
    description: "Setup Node from package.json"
    required: false
    default: "true"
  setup_ruby:
    description: "Setup Ruby from .ruby-version; bundle install when Gemfile exists (cached)"
    required: false
    default: "false"

runs:
  using: composite
  steps:
    - name: 🟢 Setup Node from package.json
      if: inputs.setup_node == 'true'
      uses: actions/setup-node@v4
      with:
        node-version-file: "package.json"
        cache: "npm"

    - name: 🔴 Setup Ruby + Bundler
      if: inputs.setup_ruby == 'true'
      uses: ruby/setup-ruby@v1
      with:
        ruby-version: ".ruby-version"
        bundler-cache: true
