# CocoaPods for Bundler cache in CI. Keep in sync with ios/Podfile.lock (COCOAPODS: line).
# Ruby version: [.ruby-version](.ruby-version) + CLI doctor — not in Gemfile so Bundler
# won't fail on patch/engine drift vs Gemfile.lock's RUBY VERSION.
source "https://rubygems.org"

gem "cocoapods", "1.16.2"
